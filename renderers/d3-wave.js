function WaveGraph() {
    this.getOptions = function() {

    };

    this.renderVisualization = function(data) {
        console.log("Rendering Visualization");
        // Parse data into rickshaw format

        // graph = new Rickshaw.Graph()

        // Beautify the graph

        // Add month names

        // Add watermark
    }
}