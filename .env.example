# PostHog Analytics
# The project API key is hardcoded (it's a public write-only key).
# Override the host to use a reverse proxy (e.g. to bypass ad blockers):
# PUBLIC_POSTHOG_HOST=https://t.savas.ca
