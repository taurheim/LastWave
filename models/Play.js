// I could prevent duplication by having two separate sets of data: Play(date, song) and Song(all the other info) - for now I'll keep it simple
function Play(){
    this.date;
    this.song;
    this.artist;
    this.album;
    this.time;
}